
# 端口号
server:
  port: 1118

spring:
  application:
    name: ml-config
  # 设置为本地方式
  profiles:
    active: ${config.native:dev}

  cloud:
    config:
      server:
        native:
          # 配置文件所在目录，classpath（类路径）和（系统文件路径） file两种
          search-locations: classpath:/config/

